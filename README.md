# ToDo
V1 of ToDo List application. It currently allows the user to Create tasks, Display (Read) tasks, Update tasks, and Delete tasks.
It currently saves tasks in a text file, but for my next build in C# I plan on doing SQLite, and then a proper API with SQL.
